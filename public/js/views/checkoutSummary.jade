h2 Shopping Cart
div(ng-controller="CartSummaryCtrl")
    .alert.alert-warning(ng-show="cartData.length == 0")
        |There are no products in your shopping cart.
        a.alert-link(href="#/products")  Return to the catalogue

    div(ng-hide="cartData.length == 0")
        table.table
            thead
                tr
                    th Quantity
                    th Item
                    th.text-right Price
                    th.text-right Subtotal
            tbody
                tr(ng-repeat="item in cartData")
                    td.text-center
                        button.btn.btn-danger.btn-circle(ng-click="decrement(item.id)")
                            span.glyphicon.glyphicon-minus(aria-hidden="true")
                        span.item-count {{item.count}}
                        button.btn.btn-success.btn-circle(ng-click="increment(item)")
                            span.glyphicon.glyphicon-plus(aria-hidden="true")
                    td.text-left {{item.name}}
                    td.text-right {{item.price | currency}}
                    td.text-right {{ (item.price * item.count) | currency}}
                    td
                        button.btn.btn-sm.btn-warning(ng-click="remove(item.id)") Remove
            tfoot
                tr
                    td.text-right(colspan="3") Total:
                    td.text-right {{total() | currency}}
        .text-center
            a.btn.btn-primary(href="#/products") Continue Shopping
            a.btn.btn-primary(href="#/placeorder") Place Order Now

a.btn.btn-primary(href="#/products") Back
